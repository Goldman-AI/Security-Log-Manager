from setuptools import setup, find_packages

setup(
    name='Security-Log-Manager',
    version='1.0.0',
    packages=find_packages(),
    install_requires=[
        'some-dependency',  # Add your package dependencies here
    ],
)